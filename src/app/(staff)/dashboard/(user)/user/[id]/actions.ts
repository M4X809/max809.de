// "use server";

// import { hasPermission } from "~/lib/utils";

// export const savePermission = async (userId: string, permission: string) => {
//     const perm = await hasPermission("editUserPermissions")
//     if (!perm) return

// }
